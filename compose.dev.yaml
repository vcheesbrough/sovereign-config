services:
  postgres:
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD from the development secret provider}
      POSTGRES_PASSWORD_FILE: ""
  sovereign-config:
    image: registry.desync.link/sovereign-config:${SOVEREIGN_CONFIG_IMAGE_TAG:-local}
    environment:
      SOVEREIGN_CONFIG_DATABASE_URL: ${SOVEREIGN_CONFIG_DATABASE_URL:?Set SOVEREIGN_CONFIG_DATABASE_URL from the development secret provider}
